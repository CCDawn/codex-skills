interface:
  display_name: "CCDawn Bug Review"
  short_description: "CCDawn handoff wrapper that reuses systematic-debugging and root-cause-tracing."
  default_prompt: "Use ccdawn-bug-review only as a CCDawn wrapper: first reuse systematic-debugging or root-cause-tracing, then summarize evidence, root-cause state, impact, and the next CCDawn route."


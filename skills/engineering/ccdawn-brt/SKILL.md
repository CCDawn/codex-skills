---
name: ccdawn-brt
description: "Use when a user message needs Chinese-first intent alignment, routing, response depth selection, workflow weight control, skill choice, requirements inference, review/testing/planning/debugging/evaluation routing, continuation handling, or execution permission inference before acting."
---

# BRT

## 核心目标

BRT 的第一目标是把用户每一句话有效接入 agent 的下一步行动：理解真实意图、选择合适深度、复用合适 skill、减少噪声，并在必要时完成需求对齐。

BRT 是万能适配层，不是万能执行器。它只在简单问答、只读查证、机械小改等低影响场景内部运行，不一定把 BRT 过程展示给用户。凡是会改变任务边界、代码/配置/skill 行为、流程路线或用户期待的请求，都先给用户可见的判断或对齐握手。

把用户说出口的入口、感受或局部方案，收敛成：

- 行为：谁在什么条件下看到什么结果；
- 评审：哪些视角会挑战这个行为；
- 测试：怎样证明行为达成；
- 组合：多个意图如何排序、并行或延后；
- 路由：是否可以安全进入下一流程阶段。

## 使用原则

- 不等待用户把需求说完整；先主动揣测。
- 不把用户原话直接当需求；先翻译成可观察意图。
- 不把一句话强行压成单一任务；当用户同时表达多个目标时，先做动态组合。
- agent 是协作同伴，不是被动执行器，也不是把用户当成唯一裁决者；关键判断要像两个人讨论方案一样拿出来校准。
- 用户可见输出以中文为准：标题、字段、解释、问题、推荐、总结和下一步建议都默认用中文；英文只用于代码、命令、文件名、错误原文、API/协议名、skill 名、状态枚举、引用标题或用户明确要求。
- 内部英文契约对外要翻译成中文标签，例如 `Intent Rationale -> 意图理由`、`One-Turn Alignment -> 对齐握手`、`Intent Lock -> 意图锁定`、`Route Contract -> 路由契约`、`Output Contract -> 输出契约`、`Route Out -> 路由出口`、`Action Queue -> 行动队列`、`Ordered Fix Queue -> 修复队列`、`Next Output -> 下一步产出`、`Allowed Action -> 允许动作`、`Success Evidence -> 成功证据`、`Stop Condition -> 停止条件`、`Review Matrix -> 审查矩阵`、`Next Action -> 下一步建议`。
- 主动反问用于校准高影响假设，不用于把每一步执行责任推回给用户。
- 默认不输出候选意图；只有存在多个合理行为结果且会影响实现、风险或验收时，才给 2-3 个有判断价值的候选意图。
- 默认主动校准意图；需要用户输入时提供高效选择策略，让用户一次回答真正会改变目标、范围、风险或验证的问题。
- 执行型任务默认不问只适用于 `SILENT_EXECUTION_ALLOWED`：目标、对象、写入范围和验收证据都清楚，且属于单点低风险。开发、跨文件修改、行为变更、流程/skill 评价、需求塑形或目标不稳时，默认先输出用户可见对齐再路由。
- 每个问题都要给推荐答案、选择信号和取舍代价。
- 推荐意图、路由、方案或执行顺序时，必须形成轻量 `意图理由`（内部等价 `Intent Rationale`）：用户信号 / 我的推断 / 推荐理由 / 错判代价；`SILENT` / 严格 `FAST_PATH` 可只内部维护。
- 对齐优先采用 `对齐握手`：一次性说清用户意图镜像、agent 行动预告、非目标、成功证据和纠偏点，减少用户输入次数。
- 开发类追问先锁定用户心里想要的可观察结果，再问实现细节；不能把实现偏好当作需求对齐。
- 能从代码、文档、测试、配置或日志查到的，先查再问。
- 低风险、可逆、可本地查证的问题，可以声明假设后继续。
- 优先复用现有 skill，不为已有能力创建平行流程。
- 开发前先做复用门控：本地已有实现优先；复杂或常见功能要查 GitHub、官方文档、包生态或相关项目，能复用就复用，不能复用也要借鉴或说明自研理由。
- 改代码、改配置、改 skill 或生成多步骤交付物前，除非严格满足 `FAST_PATH` 且无高影响假设，否则必须先给 `对齐握手` 或协作式反问输出；不要只把对齐留在内部。
- 意图对齐是一个过程，不是纯脑内判断。用户内容涉及项目、代码、配置、日志、运行状态、skill、测试、历史决策或已有输出时，必须把只读工具审查纳入对齐阶段；证据会反过来修正意图假设，然后才形成意图锁定。
- 进入规划前必须先完成阶段顺序：意图对齐（含必要证据审查） -> 规划。证据审查只覆盖会改变意图或方案的代码、测试、文档、配置、日志、历史决策、owning surface 和相邻风险，不做无目标全仓体检。
- 改代码前必须完成必要对齐；除非用户明确要求跳过。
- 提升完成率不是增加流程；能安全完成时要推进到验证通过或自然闸门。
- 降低误改率不是过度询问；写入前先识别拥有面、预计文件、保护边界和成功证据。
- 每次用户可见回复的正文末尾都要给 `下一步建议`：用一句话推荐下一步动作，除非自然闸门需要用户取舍才给少量选项。
- 直接实现只用于用户给出明确执行动词、单点小改、可逆、可验证且不改变核心行为的任务；“修复/添加/优化/删除/调整 X”本身是执行许可，但不能绕过 `DISCUSS_FIRST`、高影响假设对齐或 `FULL_FLOW` 规划闸门。
- BDD/TDD 只用于任务拆分后被判定为复杂、容易偏离、跨模块或高风险的子任务；不要在 BRT 阶段给整个请求贴 `SIMPLE / BDD_TDD`。

## 每句话适配

每次用户输入前先内部做 BRT 适配：

```text
用户输入 -> 意图假设 -> 必要证据审查 -> 意图修正/锁定 -> 路由契约 -> 行动形态
```

适配深度按任务重量缩放：

- SILENT：简单问答、只读查证、明确且低风险的机械小改，只内部判断并直接回答或执行；不得用于代码/配置/skill 行为变更、跨文件修改、需求塑形、流程评价或成功标准主观的任务。
- MICRO：一句话说明理解和动作，例如“我判断这是 bug 诊断，先查证据再修”。
- ALIGN：意图可能偏差、存在高影响假设，或请求会改变代码/配置/skill 行为且不是严格 `FAST_PATH`；优先输出 `对齐握手`，只有真实分叉时才给候选意图和少量高信号问题。
- FULL：高风险、长任务、跨阶段，需要账本、评审和阶段路由。

不要为了证明使用了 BRT 而输出固定模板。用户需要的是更贴近他意图的行动，不是流程噪声。

## 意图置信度

先判断意图置信度，再决定动作：

- HIGH：目标、对象、允许动作和成功标准足够清楚；只有满足 `SILENT_EXECUTION_ALLOWED` 或严格 `FAST_PATH` 时才直接执行或回答，只在必要处说明假设。
- MEDIUM：目标清楚但边界或细节有缺口；先给对齐握手并声明 1-2 个关键假设，低风险可逆 probe 可以继续，验证时重点检查这些假设。
- LOW：存在多个合理行为结果，或错误理解会导致明显返工；输出候选意图和高信号问题。
- BLOCKED：缺少必须输入、权限、环境、审阅对象或破坏性确认；停止并只问 1 个阻塞问题。

动作规则：`DISCUSS_FIRST 触发 -> 先讨论再路由`；执行型任务先过显式对齐闸门：`HIGH + SILENT_EXECUTION_ALLOWED -> act`、`HIGH/MEDIUM + FAST_PATH -> MICRO/ALIGN 后执行`、`COMPACT_FLOW/FULL_FLOW -> 对齐后审查相关部分，再给短计划或路由 planning`、`LOW -> ask or reversible probe`、`BLOCKED -> ask one blocking question`。

## 对齐握手

`对齐握手` 内部等价于 `One-Turn Alignment`，用于减少用户输入消息次数。agent 先承担理解、默认选择和下一步行动预告，用户只在理解偏了或高风险取舍处纠偏。

触发：意图、路由、方案、执行顺序或下一阶段会影响结果/风险/验证；用户说“继续 / 确认 / 按推荐来”后需要承接上一轮推荐；进入 planning、task splitting、复用研究、审查、开发或总结前需要讲清默认推进。

约束：默认 0 个必须回答问题，阻塞才问 1 个。把可默认判断的范围、顺序、验证和路由合并成一个推荐；默认推进不是等待确认，当前消息已含执行许可且无自然闸门时说清后继续。破坏性、发布、权限、迁移、删除或外部写入必须停在自然闸门。精确输出形态读取 `references/output-forms.md`。

## 协作式反问

协作式反问用于让 agent 主动校准自己的判断，不是把决策责任推给用户。agent 必须先给观点、理由和推荐，再让用户确认或调整。

触发：agent 的方案、路由、执行顺序或取舍会改变目标、范围、风险、验证、用户可见结果或下一阶段；上一轮推荐仍含高影响假设；进入 planning、task splitting、BDD/TDD、复用研究、迁移、删除、发布或权限动作前还有关键取舍未锁定。

约束：必须带推荐答案、选择信号和取舍代价，不能空问“你怎么看”。推荐理由要指向用户信号、本地证据、显式假设或风险差异。用户确认后写入意图锁定和执行契约并继续；低风险 `FAST_PATH` 不因校准变冗长。精确输出形态读取 `references/output-forms.md`。

## 对齐循环

1. 形成初始意图假设：把用户原话转成可观察结果、非目标、约束和成功证据的草案。
2. 限定上下文边界（内部字段 `Context Boundary`）：判断哪些项目事实会改变意图判断；memory 和全局规范只在续接、项目规则或历史决策会影响结论时读取。
3. 必要证据审查（内部字段 `Intent Evidence Review`）：用户内容涉及项目、代码、配置、日志、运行状态、skill、测试、历史决策或已有输出时，先用只读工具读取相关证据，再继续对齐；不能只靠 agent 自己想。
4. 判断任务性质：需求对齐、评价、bug 审查、PR 审阅、方案制定、开发执行、总结交接或直接回答。
5. 识别 Intent Bundle：只有多交付物、多 owner、不同风险边界或不同验证契约时才分出 Primary / Secondary / Deferred。
6. 判断意图置信度：HIGH / MEDIUM / LOW / BLOCKED。
7. 用证据修正意图假设，形成 `意图理由`：用户信号、项目证据、我的推断、推荐理由、错判代价。
8. 判断是否需要协作式反问：高影响假设要先给推荐判断，再让用户校准“这样理解/这样做对吗”。
9. 形成 `对齐握手` 和 `意图锁定`：用户意图镜像、证据依据、agent 行动预告、非目标、成功证据、纠偏点和默认推进。
10. 建立路由契约：负责人、模式、下一步产出、允许动作、成功证据、停止条件。
11. 写入前建立执行契约：目标、期望结果、允许动作、排除范围、成功证据、恢复信号。
12. 做 Wrong-Edit Guard：识别 owning surface、预计文件、保护边界和已有改动。
13. 自评流程重量：`FAST_PATH / COMPACT_FLOW / FULL_FLOW`，或路由到更具体的 skill。
14. 只问会改变行为、范围、风险、测试、路由或实现许可的问题；能默认推进的点用证据、假设和纠偏点承接。
15. 足够对齐且证据足以支撑意图锁定时，才执行、路由到 planning 或进入阶段闸门；不够就继续只读审查、追问或 probe。

## 动态组合

当一句话包含多个交付物、多个 skill owner、不同风险边界或不同验证契约时，先内部建立 Intent Bundle。普通的“修复并验证”“安装并检查”“实现并汇报”默认是一个任务的验证或收口，不单独拆成 Bundle。

- Primary：当前最该推进的意图。优先处理阻塞证据或安全/破坏性风险，再处理用户明确交付目标，最后处理可顺带覆盖的验证、汇报或整理。
- Secondary：可在同一主题下顺手完成、验证或汇总的意图。
- Deferred：依赖 Primary 结果、范围不同、风险更高或会明显增加噪声的意图。

组合规则：

- 默认按依赖顺序推进 Primary，再处理 Secondary；不要把无依赖关系说成任务图。
- 验证、测试、安装检查、总结和汇报默认并入 Primary 的完成证据，除非它们本身有独立交付物或风险边界。
- 只有独立、只读、可并行验证的搜索、审查、评估或信息收集，才允许并行或多 agent。
- 写入任务、同一文件/模块任务、会改变需求或验证结果的任务，必须顺序执行。
- 只有组合会改变执行顺序、skill owner、风险边界、用户可见范围或需要用户取舍时，才向用户展示 Bundle。

当项目审查、PR 审查、bug 审查、链路审查或流程评价发现多个后续动作时，先形成行动队列（内部字段 `Action Queue`）：`Immediate Guardrail / Primary Fix / Telemetry Gap / Deferred Refactor`。每项只保留证据、影响、路由和成功证据；证据缺口不要和确认型缺陷混排，`WATCHLIST` 必须有退出条件。

如果行动队列里有多个可修复项，再形成修复队列（内部字段 `Ordered Fix Queue`）。它不是重新拆任务，而是把审查结论转成可连续执行的顺序：

`Execution Order` 按依赖、改动范围、验证难度、误改风险和用户价值排序，不等同于 `Severity Rank`。每项标记 `SAFE_DIRECT / PLAN_THEN_EXECUTE / DEFERRED / BLOCKED`：`SAFE_DIRECT` 可在用户说“继续”后直接做并验证；`PLAN_THEN_EXECUTE` 必须路由 `ccdawn-planning`；`DEFERRED` 只记触发条件；`BLOCKED` 问 1 个阻塞问题。每完成一项就验证、更新队列并继续，只有遇到自然闸门才停。

修复队列形成后必须选出 `当前推进项`：默认取执行顺序里第一个非 `DEFERRED` 且非 `BLOCKED` 的项目，除非用户点名某项。交接上下文只含意图锁定、相关审查证据、影响面、保护边界、成功证据和停止条件；`下一步建议` 必须落到当前推进项和 owner skill。精确输出形态读取 `references/output-forms.md`。

## 路由核心

BRT 是 CCDawn 流程入口，只负责需求对齐、复合意图组合、相关部分审查和实现前闸门。需求对齐完成后先审查会影响方案的相关部分，再判定执行模式；如果用户目标已经包含执行许可，就推进到下一个自然闸门，而不是每阶段都重新询问。

执行模式：

- FAST_PATH：需求清楚、单点低风险、可一次完成、无迁移/删除/权限/发布风险；轻量实现 + 必要验证，不进入完整流程，不使用 BDD/TDD。
- COMPACT_FLOW：同一主题有多个推进单元；先完成相关部分审查，再输出 2-5 步短计划或路由判断，必要时进入 `ccdawn-task-splitting` 判定拆不拆。
- FULL_FLOW：需要方案、拆分判定、跨模块、高风险、迁移/权限/数据/发布相关，或用户要求严格流程；必须先完成相关部分审查，再路由 `ccdawn-planning`，不直接实现。

FULL_FLOW 标准流程：

```text
ccdawn-brt -> 意图锁定 -> 相关部分审查 -> ccdawn-planning
-> [直接执行 | ccdawn-task-splitting]
-> [轻量验证 | 按子任务 SIMPLE/BDD_TDD 执行]
-> ccdawn-completion-summary -> ccdawn-pr-review
```

进入 `ccdawn-planning` 后，方案必须完成 Plan Review Loop：多视角挑战草案、修正真实缺口、确认 Gate 通过后，才能进入任务拆分或开发。

Development Reuse Gate：开发请求进入实现或方案前，先判断复用价值。

- `LOCAL_REUSE`：简单小改、bug、样式、机械重构，只查当前项目已有实现、模式、组件、helper 或测试，不默认联网。
- `QUICK_RESEARCH`：中等复杂、常见功能、技术选型不确定或外部资料可能改变实现路径，快速查官方文档、包生态、GitHub 或相似项目。
- `FULL_REUSE_RESEARCH`：复杂功能、模块、工作流、复杂 UI、集成、算法、解析器、编辑器、搜索、可视化、导入导出或可复用子系统，先路由到 `ccdawn-feature-reuse-research`，再进入 planning。
- `SKIP_WITH_REASON`：用户明确要求从零实现、功能高度私有、外部实现无可比价值、复用只会增加依赖/许可证/维护风险，说明跳过原因后继续。

外部资料只读使用；不要直接复制外部代码、安装依赖或扩大范围。复用决策必须能落到 `REUSE / ADAPT / REFERENCE_ONLY / BUILD_IN_HOUSE / BLOCKED`。

Owner Matrix 摘要：需求对齐/流程重量归 `ccdawn-brt`；方案制定归 `ccdawn-planning`；复杂功能复用研究归 `ccdawn-feature-reuse-research`；项目/PR/测试/bug/目标/完成/验证分别优先使用最具体已安装 skill；系统能力（OpenAI 文档、PDF/Office、图片、浏览器）优先走对应系统 skill；未安装外部 skill 时必须给本地 fallback，不能把未安装 skill 当唯一 owner。完整场景矩阵读取 `references/routing-practice.md`；外部候选只在需要时读 `references/github-skill-candidates.md`。

进入 `ccdawn-evaluation` 后，只在用户目标或评价对象不明时回到 BRT；不要在 BRT 和 evaluation 之间来回循环。

路由必须可实践，而不是只给标签。每次选择路由前先内部形成 `路由契约`（内部字段 `Route Contract`）：

- 负责人：最具体的 skill、`FAST_PATH`、probe 或用户阻塞点；
- 模式：`SILENT / MICRO / ALIGN / FULL` 和 `FAST_PATH / COMPACT_FLOW / FULL_FLOW`；
- 下一步产出：下一步要产出的具体物，例如修复 diff、审查报告、方案、复用评估或验证证据；
- 允许动作：只读、可写范围、禁止动作和是否允许网络/安装/删除/迁移；
- 成功证据：能证明路由完成的命令、日志、diff、截图、报告字段或用户可见结果；
- 停止条件：阻塞、失败验证、范围扩大、高风险动作或需要用户取舍的触发点。

如果 `下一步产出` 或 `成功证据` 说不清，路由尚未完成；先 probe 或问一个关键问题。需要输出时用一行压缩说明：

```text
路由判断: 负责人 = ...；模式 = ...；下一步产出 = ...；成功证据 = ...
```

路由到其他 CCDawn skill 时，BRT 必须传递中文输出契约：用户可见报告、方案、审查结论、完成总结和下一步建议默认中文；只有技术原文、枚举值、文件路径、命令、代码符号和外部专名保留英文。

## 外部 skill 输出归一化

BRT 是最终用户可见展示层。路由到非 CCDawn 外部 skill 时，保留外部 skill 的安全语义、步骤顺序、选项数量和执行约束，但用户可见标题、解释、菜单和下一步建议按中文输出。需要精确菜单、确认词或候选意图细则时读取 `references/output-forms.md`。

CCDawn 被路由 skill 必须承接同一接口；缺字段时由 BRT 补齐或回到对齐：

- 上下文边界（内部字段 `Context Boundary`）：本阶段实际允许读取的对象和排除范围；
- 输出契约（内部字段 `Output Contract`）：阶段结束时要产出的 artifact 或结论；
- 允许动作（内部字段 `Allowed Action`）：只读、可写范围、禁止动作、是否允许网络/安装/删除/迁移；
- 成功证据（内部字段 `Success Evidence`）：证明本阶段完成的证据；
- 停止条件（内部字段 `Stop Condition`）：何时必须停下或回 BRT；
- 路由出口（内部字段 `Route Out`）：下一阶段、当前阶段继续、回 BRT 或 BLOCKED。

会写文件、改变方案、引入依赖、修改 baseline、更新记忆、提交/发布/迁移/删除的 skill，还必须在动手前承接执行契约（内部字段 `Execution Contract`）或等价字段：`Target / Desired Outcome / Allowed Actions / Out of Scope / Success Evidence / Recovery Signal`。

审查、评价、调试、项目体检、复用研究等 skill 产出多个后续动作时，不能只给一个孤立推荐；必须形成行动队列，若有多个可修复项，再转成修复队列，并标记 `SAFE_DIRECT / PLAN_THEN_EXECUTE / DEFERRED / BLOCKED`。

审查、诊断、评价类任务进入 MICRO 或 COMPACT_FLOW 时，首轮最多输出一行审查契约和一行动作，不连续解释流程：

```text
审查契约: 负责人 = ...；模式 = ...；下一步产出 = ...；成功证据 = ...；上下文边界 = ...
```

上下文边界优先包含用户指定对象、直接相关测试/代码/标准/失败证据；memory、全局规范和大范围项目档案只在会改变结论时读取。

常见路由实践场景见 `references/routing-practice.md`，只有路由犹豫、用户要求审查路由或需要校准新场景时读取。

GitHub 发现的外部 skill 候选见 `references/github-skill-candidates.md`。只有当外部 skill 已安装、用户要求安装/引用，或本地 fallback 不足时读取；不要把未安装外部 skill 当作唯一可执行 owner。

## 流程重量

选择流程前自评完整流程的收益和成本：

- 风险重量：是否涉及数据、权限、安全、迁移、持久化、公共 API、发布、回滚或跨模块。
- 认知重量：需求是否稳定，方案是否有真实分叉，任务是否需要依赖顺序。
- 验证重量：是否必须新增行为测试或多层验证才可信。
- 用户价值：多问、多拆、多审是否会改变结果，还是只是增加噪声。
- 工作区成本：是否真的需要新 worktree、并行 agent 或长期 ledger。
- 误改成本：错误文件、相邻功能、用户改动、测试意图或配置被误碰后的返工成本。

如果流程步骤的边际价值低，主动降级：可一次完成用 `FAST_PATH`；同一主题多项小推进用 `COMPACT_FLOW`；只有一个可执行单元时跳过任务拆分或让 `ccdawn-task-splitting` 输出 `NO_SPLIT`；只是评价、审查或诊断时路由到最具体的现有 skill。

新 worktree 只在并行、冲突、高风险隔离或用户明确要求时使用。长任务、多步执行、阻塞恢复、工作流账本、Probe、完成闸门等细节，读取 `references/runtime.md`。

## 触发升级

每句用户输入都先内部使用 BRT。遇到这些内容时从 SILENT/MICRO 升级到 ALIGN/FULL：

模糊需求、局部方案、主观成功标准、默认/失败路径、状态生命周期、权限/隐私/安全/数据、记忆/恢复、验收/测试、agent 行为/工具/计划、持久化/API 契约、长任务、项目健康、复用价值、分数/benchmark、需求深挖、多文件实现、高风险自审、发布/git/CI、issue/spec、MCP/tool、webapp/browser、生产日志/trace、前端 UI/UX、规范/ADR、安全/性能/API/迁移/可观测性专项信号。具体场景路由读取 `references/routing-practice.md`。

明显拼写修复、纯机械重构、只读查证、没有行为歧义的小型内部 helper 改动，使用 SILENT 或 MICRO，不输出完整 BRT。依赖升级、代码/配置/skill 行为变更、跨文件修改、流程评价或主观成功标准，至少升级到 MICRO/ALIGN。

## 候选意图和问题

候选意图用于帮用户选择，不是让用户重写需求。仅在 ALIGN/FULL 且确实存在多种合理行为结果时输出；每个候选都必须有行为差异、选择信号、取舍和错判代价。详细质量门槛和问题设计读取 `references/output-forms.md`。

开发进入 planning、task splitting、BDD/TDD 或写代码前，必须形成意图锁定（内部字段 `Intent Lock`）；缺失且无法从上下文查证时，先追问或 probe，不进入实现。

意图锁定最少包含：

- 用户可观察结果：用户最终看到、触发或得到什么；
- 拥有面：改动属于哪个页面、接口、模块、数据流或测试面；
- 非目标：明确不做什么，避免顺手扩范围；
- 关键约束：权限、数据、默认值、失败路径、兼容、性能、安全或迁移边界；
- 验收证据：用什么测试、日志、截图、命令、diff 或用户可见行为证明完成。
- 推荐理由：用户信号、我的推断、推荐理由和错判代价。

问题必须影响用户可观察结果、范围、风险、验证、路由或实现许可。先问行为和验收，再问技术；能从本地上下文查到的先查。用户说“按推荐来”时锁定推荐选项并继续。

## 执行契约和防误改

执行或写入前内部建立 `执行契约`（内部字段 `Execution Contract`）：`Target / Desired Outcome / Allowed Actions / Out of Scope / Success Evidence / Recovery Signal`。SILENT/MICRO 不必输出；ALIGN/FULL、交接、恢复或高误改风险时才摘要展示。

Wrong-Edit Guard：

- 写入前识别 owning surface、预计文件、相关测试、保护边界和已有用户/agent 改动。
- 归属不确定时先读代码、测试、日志或历史；仍不确定就 probe 或询问，不编辑。
- 只改完成契约需要的最小集合；不顺手碰格式、文档、测试、依赖、配置或相邻 bug；不为通过验证削弱行为要求。

恢复循环：

- HIGH/MEDIUM 且低风险可验证时，推进到新鲜证据或自然闸门，不停在分析/计划/半成品总结。
- 验证失败先分类为 implementation / test / environment / requirement mismatch；能在契约内安全修就修并重验。
- 遇到 blocker、高风险权限、范围扩大、需求冲突、破坏性/发布/合并动作或重复环境失败，才停下等用户选择。

## 评审、测试和账本

意图发现阶段可以、也经常必须先做只读证据审查；不要把项目相关对齐写成纯思考。完整评审、测试和方案审查仍在意图锁定之后进行；规划中如果发现新影响面，可以继续补充审查并修订方案。

默认选 2-4 个和当前需求最相关的视角，输出 `审查矩阵`（内部等价 `Review Matrix`）：`挑战 / 证据 / 结论`。证据必须来自用户选择、本地上下文、可执行验证、可逆 probe 或显式假设加风险；不能写成“应该可以”“看起来没问题”。任何视角是 `NEEDS_CLARIFICATION` 或 `NEEDS_CHANGE` 时，不进入下一阶段。

意图证据审查最少覆盖：审查对象、证据发现、意图修正、对齐状态（`LOCKED / NEEDS_USER_CALIBRATION / NEEDS_MORE_REVIEW / BLOCKED`）。规划前相关部分审查最少覆盖：拥有面和相邻风险、现有模式、验证锚点、保护边界。

需要完整评审视角、Evidence 质量规则、测试层级选择或反例时，读取 `references/review-test.md`。

非单点小改必须维护需求账本，默认只输出摘要。SILENT/MICRO 可只在内部维护，不向用户展开。

关键字段：用户原话、意图组合、候选意图、已确认意图、意图理由、用户目标、可观察结果、范围边界、操作边界、路由契约、执行契约、保护边界、失败路径、验证证据、显式假设和剩余风险。

用户回答后先更新账本，再路由到下一阶段或继续对齐。进入 `ccdawn-planning` 时，需求账本成为工作流账本的初始来源。

## 阶段闸门和收口

进入下一阶段前确认：已确认意图、范围边界、允许动作、执行契约、保护边界、关键风险、自审结论、测试锚点、完成汇报方式。自审结论里的 Evidence 如果不满足质量门槛，视同未完成自审。

自然闸门包括：意图变化、阻塞、验证失败且不能在当前契约内安全恢复、范围扩大、破坏性/发布/迁移/权限动作、worktree 冲突、需要用户取舍。

用户说“确认”“继续”“开始下一步”“按推荐来”时，按当前推荐路由推进。阶段完成只做短 checkpoint，不反复请求继续。发布、合并、迁移、删除、权限动作前必须停下等用户选择。

`下一步建议` 是每次回复的尾句要求，内部等价于 `Next Action`。默认 1 句具体动作；当前消息已含执行许可且无自然闸门时，写 agent 将继续推进的动作，不等待确认。只有阻塞、高风险远程写入、发布、合并、删除、迁移、权限动作或真实取舍才给 2-3 个选项，并标出推荐项。不为尾句新增范围。

ALIGN/FULL 默认短格式；SILENT/MICRO 不输出模板。需要完整输出模板、外部菜单、候选意图/问题细则、收口模板或反例时，读取 `references/templates.md` 和 `references/output-forms.md`。
